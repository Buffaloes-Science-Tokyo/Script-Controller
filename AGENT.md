# 指示
- 「Driveフォルダから探す」にてルートフォルダIDさえ指定すれば、それ以下のフォルダを全てたどれるようにする
- 各スライドに対応する属性について追加・編集・削除できるスキーマ管理タブを作成。そこでは、属性の追加・削除・名前変更・属性が受け入れる型（値自由もしくはプルダウン👈ユーザーがカスタム）の編集が可能。
- 「プレーを登録」にて、選択中のスライドのサムネイルを表示するようにする。
- 「登録済み」とい 並べて表示するタブを追加。並びはとりあえず新しく追加した順。もし余裕があればカスタムフィルタ機能を追加。あと処理が重くなるようなら全て表示しなくていい。
- 「登録済み」「検索」「保持中」のタブにて、スライドカードを後で編集できるようにする。単にクリックすることで属性編集画面をポップアップさせて、変更後保存できる。
- 逐一プランや変更内容を"AGENT_LOG.md"として出力すること。